{% extends '_layout.html.twig' %}

{% block title %}Wild Circus | Spectacle{% endblock %}
    {% block stylesheets %}
        {{ parent() }}
        {{ encore_entry_link_tags('accueil') }}
    {% endblock %}
{% block body %}
    <section class="w-100 d-flex flex-column justify-content-center align-items-center bg-primary py-5">
        <h1 class="text-center my-5">Nos spectacles</h1>
        <p class="text-light col-10 text-center">Créé en 1991, Le cirque sauvage se produit dans le monde entier contre toute attente depuis 29 ans. Nous vous proposons un spectacle exclusivement dirigé par une troupe d'artistes talentueux, avec nous pas d'animaux !!! ou presque ... 69 artistes de tous horizons, présenteront un spectacle de 2 heures. parmi eux, il y aura:</p>
        <div class="d-flex flex-column col-12 p-0 justify-content-center align-items-center my-4">
            {% for show in  shows %}
                <div class="card col-lg-4 message p-0 border-0 align-items-center my-3">
                    <div class="card-img-top text-center col-12 p-0">
                        <img class="w100" src="{{ show.picture }}" alt="">
                    </div>
                    <h3 class="col-12 p-0 bg-red text-dark text-center m-0 rounded-top">{{ show.title }}</h3>
                    <p class="text-center col-10">{{ show.description }}</p>
                </div>
            {%  endfor %}
        </div>
    </section>


{% endblock %}

