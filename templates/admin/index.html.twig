{% extends '_layout.html.twig' %}

{% block title %}Wild circus | admin | Gestion accueil{% endblock %}
    {% block stylesheets %}
        {{ parent() }}
        {{ encore_entry_link_tags('accueil') }}
    {% endblock %}
{% block body %}
    <section class="w-100 d-flex flex-column justify-content-center align-items-center bg-primary">
        <h1>Gestion de l'administration'</h1>
        <div class="row mb-4 justify-content-around mt-4 col-12 p-0">
            <div class=" card m-4 col-md-4">
                <div class="card-body">
                    <h3 class="text-center card-title">Gestion accueil</h3>
                    <p class="text-center card-text">message d'accueil</p>
                    <div class="text-center mt-2"><a
                                href="{{ path('home_message_index') }}"
                                class="btn btn-red">Aller</a></div>
                </div>
            </div>
            <div class=" card m-4 col-md-4">
                <div class="card-body">
                    <h3 class="text-center card-title">Gestion Spectable</h3>
                    <p class="text-center card-text">spectacle</p>
                    <div class="text-center mt-2"><a
                                href="{{ path('show_index') }}"
                                class="btn btn-red">Aller</a></div>
                </div>
            </div>

        </div>
        <div class="col-12 p-0 text-center">
            <a class="btn btn-red col-md-3 mt-4 text-center" href="{{ path('hard') }}">Retour accueil</a>
        </div>

    </section>

{% endblock %}

